/*
13- contruir app aonde se envia um numero e ve se é par ou impar
*/
// Declaração de variáveis com Entrada de Dados.
let num = window.prompt("Digite um número: ");

// Declaração de variáveis e operações.
if (num % 2 == 0) {
    let msg = "O número " + num + " é par."
    window.alert(msg);
} else {
    let msg = "O número " + num + " é impar."
    window.alert(msg);
}


// Saída de Dados no Alert do Navegador.
window.alert(res);